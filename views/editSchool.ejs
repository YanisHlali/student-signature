<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Modifier une École</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <header class="bg-dark text-white text-center py-3 mb-4">
        <h1>Modifier une École</h1>
    </header>
    <div class="container">
        <form action="/schools/<%= school[0].id %>/edit?_method=PUT" method="POST">
            <div class="form-group">
                <label for="name">Nom de l'École</label>
                <input type="text" class="form-control" id="name" name="name" value="<%= school[0].name %>" placeholder="<%= school[0].name %>" required>
            </div>
            <div class="form-group">
                <label for="city">Ville</label>
                <input type="text" class="form-control" id="city" name="city" value="<%= school[0].city %>" placeholder="<%= school[0].city %>" required>
            </div>
            <button type="submit" class="btn btn-primary">Modifier</button>
            <a href="/schools" class="btn btn-secondary">Retour à la liste des écoles</a>
        </form>
    </div>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
